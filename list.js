document.getElementById('btn-Neymar').addEventListener('click', function () {
    const player_name = document.getElementById('name-Neymar');
    const name = player_name.innerText;

    const selectList = document.getElementById('selectList');

    const p = document.createElement('p');
    p.innerText = name;

    selectList.appendChild(p);
})


document.getElementById('btn-mbappe').addEventListener('click', function () {
    const player_name = document.getElementById('name-Kylian');
    const name = player_name.innerText;

    const selectList = document.getElementById('selectList');

    const p = document.createElement('p');
    p.innerText = name;

    selectList.appendChild(p);
})

document.getElementById('btn-Machado').addEventListener('click', function () {
    const player_name = document.getElementById('name-Machado');
    const name = player_name.innerText;

    const selectList = document.getElementById('selectList');

    const p = document.createElement('p');
    p.innerText = name;

    selectList.appendChild(p);
})

document.getElementById('btn-Sanches').addEventListener('click', function () {
    const player_name = document.getElementById('name-Sanches');
    const name = player_name.innerText;

    const selectList = document.getElementById('selectList');

    const p = document.createElement('p');
    p.innerText = name;

    selectList.appendChild(p);
})

document.getElementById('btn-Ramos').addEventListener('click', function () {
    const player_name = document.getElementById('name-Sergio');
    const name = player_name.innerText;

    const selectList = document.getElementById('selectList');

    const p = document.createElement('p');
    p.innerText = name;

    selectList.appendChild(p);
})


document.getElementById('btn-Ron').addEventListener('click', function () {
    const player_name = document.getElementById('name-Ron');
    const name = player_name.innerText;

    const selectList = document.getElementById('selectList');

    const p = document.createElement('p');
    p.innerText = name;

    selectList.appendChild(p);
})

document.getElementById('btn-Forlan').addEventListener('click', function () {
    const player_name = document.getElementById('name-Forlan');
    const name = player_name.innerText;

    const selectList = document.getElementById('selectList');

    const p = document.createElement('p');
    p.innerText = name;

    selectList.appendChild(p);
})


document.getElementById('btn-Bale').addEventListener('click', function () {
    const player_name = document.getElementById('name-Bale');
    const name = player_name.innerText;

    const selectList = document.getElementById('selectList');

    const p = document.createElement('p');
    p.innerText = name;

    selectList.appendChild(p);
})


document.getElementById('btn-Messi').addEventListener('click', function () {
    const player_name = document.getElementById('name-Messi');
    const name = player_name.innerText;

    const selectList = document.getElementById('selectList');

    const p = document.createElement('p');
    p.innerText = name;

    selectList.appendChild(p);
})